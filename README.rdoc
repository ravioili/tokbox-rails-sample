== README

Sample web app that consumes TokBox's API using the Ruby SDK. The service will create sessions and issue authorization tokens. 
Included is also a simple workflow at session/index to create, delete, and join a session.

* Ruby version

* Configuration
** Use bundler to install gems.

* Database initialization
** Migrate to latest using 'rake db:migrate'

